{
  "name": "simple-blog-cucumber-tests",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "test": "mkdir -p results && cucumber-js features/**/*.feature --import features/step_definitions/**/*.js --import support/**/*.js --format progress --format json:results/cucumber-report.json --publish-quiet"
  },
  "dependencies": {
    "@cucumber/cucumber": "11.3.0",
    "selenium-webdriver": "4.31.0"
  }
}
